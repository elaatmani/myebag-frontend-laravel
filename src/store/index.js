import { createStore } from 'vuex'
import theme from './modules/theme'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    theme
  }
})
